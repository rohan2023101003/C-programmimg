#include <stdio.h>
int main()
{
    int a,i;
    for ( i = 2; i < a; i++)
    {
       if (a%i == 0)
       {
        printf("the number %d id not prime",a);
       }
       else{
        printf("the number %d is a prime number",a);
       }
    }
   return 0; 
}