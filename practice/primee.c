#include <stdio.h>
int main()
{
    int a,i = 0;
    scanf("%d", & a);
    for ( i = 0; i < ; i++)
    {
        
    }
    
    {
        if(a%i == 0);
        printf("not prime");
        {
            else ;
            printf("prime");
        }
    }
    return 0;
}